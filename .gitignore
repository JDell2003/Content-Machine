# ---- secrets: NEVER commit ----
# NB: no trailing inline comments on pattern lines - git treats them as part of
# the pattern, which silently un-ignores the file.
.env
postiz/.env
*.env.local
# URL.txt contains the login PIN
URL.txt
# feedback.jsonl carries transcript excerpts from private video
feedback.jsonl
performance.jsonl

# ---- data: private source video, transcripts, rendered clips ----
data/
postiz/data/

# ---- third-party clones ----
vendor/

# ---- python ----
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ---- media / scratch ----
*.mp4
*.mov
*.mkv
*.wav
*.srt
*.jpg
*.png
*.log

# ---- os / editor ----
Thumbs.db
desktop.ini
.DS_Store
.idea/
.vscode/

# ---- your brand dossier: PRIVATE, and this repo is public ----
# These hold real positioning and real business numbers (BRAND-CORE.md states
# revenue and client count outright). The code runs fine without them - it
# treats each as optional and falls back to an empty section. Keep them local.
BRAND-CORE.md
BRAND-INTERVIEW.md
EXEMPLARS.md
MACHINE-VOICE.md
exemplars/
*.bak
